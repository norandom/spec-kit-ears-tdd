## Requirement-to-design translation

Map each `REQ-NNN` to a design decision and verification approach. Do not copy EARS sentences or
requirement IDs into production code. Tests may reference requirement IDs; production code may not.

Update the feature's `traceability.toml` with the final automated test selectors. A manual
verification entry MUST explain why automation is not practical.

