schema_version = "1.0"

# Repointed from the deleted Python tests to the Rust implementation. Where a conformance case
# exercises the requirement, the mapping names that case rather than the runner: the case is the
# thing that would have to change if the behaviour changed, and it survives a reimplementation.

[requirements.REQ-001]
verification = "automated"
tests = ["conformance/cases/markdown-forms/expected.json"]

[requirements.REQ-002]
verification = "automated"
tests = ["conformance/cases/fenced-code-block/expected.json"]

[requirements.REQ-003]
verification = "automated"
tests = ["conformance/cases/duplicate-identifier/expected.json"]

[requirements.REQ-004]
verification = "automated"
tests = ["conformance/cases/wide-identifier/expected.json"]

[requirements.REQ-005]
verification = "automated"
tests = ["conformance/cases/all-features-scope/expected.json"]

[requirements.REQ-006]
verification = "automated"
tests = ["conformance/cases/stale-feature-pointer/expected.json"]

[requirements.REQ-007]
verification = "automated"
tests = ["conformance/cases/all-features-scope/expected.json"]

[requirements.REQ-008]
verification = "automated"
tests = ["crates/ears-sdd/tests/conformance.rs::corpus_matches_expected_results"]

[requirements.REQ-009]
verification = "automated"
tests = ["conformance/cases/selector-traversal/expected.json"]

[requirements.REQ-010]
verification = "automated"
tests = ["conformance/cases/undecodable-spec/expected.json"]

[requirements.REQ-011]
verification = "automated"
tests = ["crates/ears-sdd/tests/read_only.rs::no_gate_modifies_the_project"]

[requirements.REQ-012]
verification = "automated"
tests = [
  "conformance/cases/modal-in-quoted-literal/expected.json",
  "crates/ears-sdd/src/ears.rs::tests::balanced_literal_is_masked",
]

[requirements.REQ-013]
verification = "automated"
tests = ["conformance/cases/feature-qualified-ids/expected.json"]

[requirements.REQ-014]
verification = "automated"
tests = ["conformance/cases/feature-qualified-ids/expected.json"]

[requirements.REQ-015]
verification = "automated"
tests = ["crates/ears-sdd/tests/conformance.rs::every_case_has_a_fixture_and_an_expected_result"]

[requirements.REQ-016]
verification = "automated"
tests = ["crates/ears-sdd/tests/conformance.rs::corpus_matches_expected_results"]

[requirements.REQ-017]
verification = "automated"
tests = ["conformance/cases/selector-missing-test/expected.json"]

[requirements.REQ-018]
verification = "automated"
tests = ["conformance/cases/selector-bare-file/expected.json"]
