.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
*.py[cod]
dist/
.coverage

# Working notes and review output. Excluded from the repository proper; also outside
# production_roots and spec_glob, so the EARS gates never read it.
review/

target/
