schema_version = "1.0"

[effects]
allow_write = { conflicts_with = ["block_write"] }
block_write = { conflicts_with = [] }

[requirements.REQ-001]
when = "operating-mode == 'maintenance'"
then = "block_write"
