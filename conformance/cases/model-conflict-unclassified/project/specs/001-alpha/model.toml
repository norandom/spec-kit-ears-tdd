schema_version = "1.0"

[effects]
persist = { conflicts_with = ["reject"] }
reject = { conflicts_with = [] }

[requirements.REQ-001]
when = "operating-mode == 'maintenance'"
then = "persist"

[requirements.REQ-002]
when = "operating-mode == 'maintenance'"
then = "reject"
